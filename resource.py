# query_update = 'UPDATE  store  SET id=?, price=?,quantity=? WHERE name=?'
            # cursor.execute(query_update,
            #                [parse_items['id'], parse_items['name'], parse_items['price'],name])